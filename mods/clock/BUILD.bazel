load("//engine:defs.bzl", "engine_mod")

# Declares the Clock that bootstrap mods publish. Runs nothing.
engine_mod(
    name = "clock",
    srcs = ["lib.rs"],
    interface = ["components.rs"],
    visibility = ["//visibility:public"],
)

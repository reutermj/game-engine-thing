load("//engine:defs.bzl", "engine_mod")

engine_mod(
    name = "lockstep",
    srcs = ["lib.rs"],
    mod_deps = ["//engine/std/clock"],
    resident = True,
    visibility = ["//visibility:public"],
)
